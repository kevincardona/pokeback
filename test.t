

function poke() {
  list buttons = document.getElementsByClassName("_42ft");

  for (var e: buttons) {
      if (e.getAttribute("ajaxify").substring(0,28) == "/pokes/inline/?dom_id_replace" && !e.getAttribute("ajaxify").indexOf("suggestion"))

  }
}

setInterval(function(){

});
}, 5000);
